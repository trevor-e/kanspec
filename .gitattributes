.kanspec/proposals/**/comments.jsonl merge=union
# Match rustfmt's Unix newline setting on Windows checkouts too.
*.rs text eol=lf
# These byte-preservation fixtures intentionally mix LF, CRLF, BOMs and no EOF newline.
tests/fixtures/frontmatter/** -text
