// S8: the board SPA — vanilla JS, no build step. Fetches /api/board, subscribes to
// /events (SSE payload is {rev, n} only; the page refetches), renders the tabs.
console.log('kanspec: assets/app.js is a wave-0 placeholder (owner: S8)');
